services:
  bot:
    build: .
    env_file: .env
    volumes:
      - ${PROJECTS_BASE_PATH:-.}:/projects
    environment:
      - PROJECTS_BASE_PATH=/projects
      - OPENCODE_BIN=opencode
    restart: unless-stopped
